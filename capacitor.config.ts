import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.dysperse.my",
  appName: "dysperse",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
